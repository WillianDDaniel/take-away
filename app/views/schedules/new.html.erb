<section class="flex flex-col w-full items-center">

  <% if @schedule.errors.any? %>

    <ul class="flex flex-wrap w-10/12 justify-center gap-2 p-1 m-2 text-red-900 bg-red-100 font-semibold rounded-md">
      <% @schedule.errors.full_messages.each do |msg| %>
        <li class="px-5">=> <%= msg %></li>
      <% end %>
    </ul>

  <% end %>

  <h1 class="w-10/12 text-center p-2 bg-gray-300 rounded-md mt-3 font-semibold">
    Cadastrar novo horário
  </h1>

  <p class="w-10/12 text-center p-5 bg-gray-200 rounded-md">
    Crie um novo horário de funcionamento para o seu restaurante. <br>
    Os horários devem ser inseridos no formato de 12 horas, com AM/PM (ex.: 9:30 AM ou 10:00 PM).
  </p>

  <%= render 'form' %>

</section>